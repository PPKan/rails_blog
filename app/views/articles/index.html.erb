<h1>Articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
      <%# /articles/#{article.id} equals to article_path(article) %>
      <%= link_to article.title, article %>
    </li>
  <%end%>
</ul>
